# Variabili d'ambiente per l'accesso alle API dei modelli LLM
# Copia questo file in '.env' e sostituisci i valori segnaposto con le tue chiavi API reali.

# Google Gemini (RICHIESTO): https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=tua-chiave-qui

# Groq (RICHIESTO per Llama e Qwen): https://console.groq.com/keys
GROQ_API_KEY=tua-chiave-qui

# Azure OpenAI (RICHIESTO per GPT-4o-mini): https://portal.azure.com
AZURE_OPENAI_ENDPOINT=tuo-endpoint
AZURE_OPENAI_KEY=tua-chiave-qui

# ============================================================
# CONFIGURAZIONE PREDEFINITA
# ============================================================
DEFAULT_MODEL_TEMPERATURE=0.2
DEFAULT_K_GENERATIONS=5
DEFAULT_MAX_TOKENS=1024
DEFAULT_K_GENERATIONS=5

# Paths
DATASET_PATH=./code/data
RESULTS_PATH=./results
LOG_PATH=./logs
